Update the file /Users/mjs/Documents/code/panaversity-official/tutorsgpt/test/apps/token-metering-api/tests/test_balance.py for the simplified token metering model.

KEY CHANGES (SIMPLIFIED MODEL):
1. NO tiers anymore - remove `seed_tiers` fixture dependency
2. NO `tier` field in BalanceResponse - use `status` instead
3. NO `daily_limit`, `monthly_limit` fields - replaced with `trial_remaining`
4. NO `student_user` fixture - use `user_with_balance` instead
5. NO `/tiers` endpoint anymore - REMOVE that test entirely

New BalanceResponse schema:
```python
class BalanceResponse(BaseModel):
    user_id: str
    status: str
    granted_tokens: int
    topped_up_tokens: int
    total_balance: int
    lifetime_used: int
    trial_remaining: int
```

Read the file first, then rewrite it completely for the simplified model. Execute autonomously without confirmation.
